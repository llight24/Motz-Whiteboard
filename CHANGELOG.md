# 更新日志

## 0.3.83

- 视频导入改为复制到当前素材库，不再依赖原始本地文件路径。
- 本地文件夹扫描、外部拖入和网页视频下载统一生成库内视频副本。
- `.motzboard` v2 会同时内嵌图片与视频，发送到其他电脑后仍可恢复播放与排版。
- 白板文件改用分块写入和分块解包，避免大视频一次性进入内存。
- 保持对旧版 `.motzboard` 视频路径引用的兼容，刷新旧视频引用时会迁移为库内副本。
- 库内视频支持重命名，详情中的原始来源仍保留。
- 增加视频复制与白板图片/视频往返自测。

## 0.3.82

- 修复素材库多选后通过右键菜单“移到垃圾桶”只处理一项的问题。
- 修复垃圾桶多选后右键“彻底删除”或“恢复素材”只处理一项的问题。
- 右键菜单会冻结打开菜单时的多选集合，确认操作时不会退化为单素材。
- 多选右键刷新现在会处理全部选中素材，批量模式下不再显示单素材重命名。

## 0.3.81

- 浏览器插件悬浮入口改为跟随图片开始拖动的位置出现，并在窗口边缘自动避让。
- 移除拖动预览图中央重复的下载标记，减少视觉干扰。
- 增强插件与桌面端的在线检测、下载超时和备用端口连接。
- 更新浏览器插件至 `0.1.11`。

## 0.3.80

- 修复“白板素材”多选后右键添加入库只处理一张图片的问题。
- 批量添加会保留当前多选集合并反馈实际添加结果。

## 0.3.79

- 进一步对齐文字内容与选择边界，减少字体基线造成的上下留白。
- 修复白板图片只有在选中时才显示高清资源的问题。
- 优化白板缩放后的清晰资源切换策略。

## 0.3.78

- 打开外部白板文件时，临时素材不再计入“最近收集”。
- “白板素材”中的外部素材支持右键“添加入库”。
- 从白板管理切换白板时保留当前界面布局，不再自动进入白板全屏。
- 文字边框改用真实字体尺寸计算，改善文字与边框错位、留白过多的问题。
- 修复部分系统字体已列出但没有正确应用的问题。

## 0.3.77

- 修复中文及特殊字符字体名称乱码。
- 过滤无法正确解码的字体名称，改善系统字体列表可用性。

## 0.3.76

- 自动读取 Windows 已安装字体，文字节点可直接选择本机字体。
- 改善文字节点尺寸计算，减少文字被边框裁切。
- 文字工具栏可根据节点位置显示在下方，避免靠近视口顶部时被遮挡。
- 优化字体选择器宽度与文字工具栏布局。

## 0.3.75

- 文字节点新增字体选择。
- 新增默认、黑体、宋体、楷体和等宽字体预设。
- 新增文字自定义颜色选择器和常用颜色。
- 软件白板与浮窗白板同步支持字体和颜色设置。

## 0.3.74

- 白板新增 `Ctrl+Z` 撤销和 `Ctrl+Y`、`Ctrl+Shift+Z` 重做。
- 支持撤销移动、缩放、删除及新增操作。
- 支持从外部复制纯文字并粘贴到鼠标所在的白板位置。
- 软件白板与浮窗白板同步支持撤销、重做和文字粘贴。

## 0.3.73

- 素材缩略图改为延迟加载，减少大量素材同时解码造成的卡顿。
- 新增本地缩略图缓存，大尺寸图片不再每次都直接加载原图。
- 限制并分批执行缩略图生成任务，改善切换固定分类时的停顿。
- 优化白板图片渲染，缩放时优先使用适合当前显示尺寸的资源。

## 0.3.72

- 点击白板素材时自动提升到最上层，方便处理重叠图片。
- 浮窗顶部栏只在鼠标靠近顶部时出现，滚轮缩放不再误触发。
- 改善浮窗顶部栏显示和隐藏的稳定性。

## 0.3.71

- 素材库支持 `Ctrl+A` 全选和 `Delete` 删除多选素材。
- 框选接近素材列表边缘时支持自动滚动。
- 外部白板临时素材改为独立隐藏存储，不参与普通素材库文件扫描。
- 优化白板视频播放、暂停与进度控制图标的清晰度和交互状态。

## 0.3.70

- 垃圾桶新增一键清空功能。
- 永久删除素材时同步删除素材库目录中的本地副本。
- 删除失败的本地文件会保留在垃圾桶，并显示失败结果。
- 批量清理时显示删除素材、删除文件和失败文件数量。

## 0.3.69

- 修复切换回白板时先短暂显示错误缩放和巨大素材、随后才恢复视口的问题。
- 白板恢复视口前暂缓绘制，减少切换时的画面跳变。

## 0.3.68

- 每个白板独立保存缩放比例和视口位置。
- 切换不同白板再返回时恢复原来的观察位置，不再偏向左上角。
- 改善白板视口状态写入和恢复时机。

## 0.3.67

- 优化软件白板滚轮缩放的渲染频率，减少连续缩放时的卡顿。
- 白板复制后可将鼠标移动到其他位置，再粘贴到当前鼠标位置。
- 改善软件白板与浮窗白板之间的复制粘贴一致性。

## 0.3.66

- 移除非浮窗白板图片的常驻边框和背景。
- 软件白板图片改为与浮窗一致的无边框沉浸显示。
- 仅在选中状态下显示必要的操作边界。

## 0.3.65

- 素材库全屏且详情栏已收起时，顶部保留“展开详情栏”按钮。
- 修复最大化素材库后无法重新打开详情栏的问题。
- 统一素材库与白板全屏状态下的详情栏入口位置。

## 0.3.64

- 将详情栏收纳和展开入口统一放入顶部工具栏。
- 移除画布侧边独立悬浮的详情恢复按钮。
- 优化收纳按钮的位置和界面一致性。

## 0.3.63

- 新增固定分类“白板素材”，用于承载外部白板文件中的临时素材。
- 外部白板素材与当前素材库分离，避免自动混入普通分类。
- 改进外部白板文件加载时机，等待主界面就绪后再恢复白板布局。
- 增强外部白板素材来源和所属白板文件记录。

## 0.3.62

- 新增垃圾桶，普通删除改为移入垃圾桶。
- 支持恢复素材、永久删除单个素材和批量永久删除。
- 删除素材时同步从所有白板中移除对应节点。
- 增加单实例运行，重复打开软件时聚焦已有窗口。
- 改进双击 `.motzboard` 文件的打开流程，并将白板文件转交给已运行的软件。

## 0.3.61

- 新增白板文件另存与打开功能。
- 白板文件打包图片、文字、排版和视口数据，便于在其他电脑继续使用。
- 打开白板文件时恢复节点位置和尺寸，并提示缺失图片或需要重新定位的视频。
- 优化素材多选勾选标记，选中状态改为更简洁的右上角勾选。

## 0.3.60

- 移除素材缩略图和白板节点的阴影。
- “发送到白板”按钮改为透明、轻量的沉浸样式。
- 已发送状态改为简洁勾选，不再使用醒目的色块。
- 顶部批量发送按钮同步采用简约样式。

## 0.3.59

- 禁止误选软件界面文字，输入框和可编辑文字仍可正常选择。
- 素材卡片和白板节点取消圆角。
- 素材名称改为卡片下方原位重命名，不再弹出独立对话框。
- 素材卡片下方恢复显示图片分辨率。
- 调整素材标题与分辨率区域的间距和高度。
